{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 4001"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "grunt": "^1.6.1"
  }
}
